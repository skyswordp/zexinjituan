# 红单功能统计报告

## 概览
红单功能（PushOrder/推单）是一个完整的后端功能模块，主要用于管理体育赛事的推单信息、AI推单用户管理等功能。

---

## 🔴 核心接口（9个） - 四层业务链路分析

### 📊 业务流程链路：创建 → 生成 → 发布 → 展示

```
┌─────────────────────────────────────────────────────────────────────┐
│ 第1层：创建 → 配置AI推单用户                                          │
│ /api/friend/pushOrder/aiUser/save [后台管理]                         │
├─────────────────────────────────────────────────────────────────────┤
│ 第2层：生成 → 触发AI内容生成                                          │
│ /api/friend/pushOrder/aiUser/backPushOrder [后台管理] 🔥🔥🔥        │
├─────────────────────────────────────────────────────────────────────┤
│ 第3层：发布 → 新增/更新推单内容                                       │
│ /api/friend/pushOrder/contentManagement/insert [后台管理] 🔥🔥🔥    │
│ /api/friend/pushOrder/contentManagement/updateState [后台管理] 🔥🔥  │
├─────────────────────────────────────────────────────────────────────┤
│ 第4层：展示 → 用户端查询与展示                                        │
│ /api/public/pushOrder/findPersonalInfoPageList [客户端/三方] 🔥🔥🔥 │
│ /api/public/pushOrder/findMonthlyRankingPageList [客户端/三方] 🔥🔥🔥│
│ /api/public/pushOrder/findPlanDetailed [客户端/三方] 🔥🔥🔥          │
│ /api/public/pushOrder/findMasterDetailed [客户端/三方] 🔥🔥          │
└─────────────────────────────────────────────────────────────────────┘
```

### 🎯 按用户群体分类 - 三类接口全覆盖

#### 第1类：后台管理类接口（6个）- **后台运营/配置人员**
需认证 | 仅内部使用

| # | 接口路径 | 工程模块 | 说明 | 优先级 |
|---|---------|---------|------|--------|
| 1 | `/api/friend/pushOrder/aiUser/save` | dc-api-office | 创建推单AI用户（配置推单者） | 🔥🔥 |
| 2 | `/api/friend/pushOrder/aiUser/update` | dc-api-office | 更新推单AI用户配置 | 🔥 |
| 3 | `/api/friend/pushOrder/aiUser/backPushOrder` | dc-api-office | **后台推单创建**（触发AI生成推单内容） | 🔥🔥🔥 |
| 4 | `/api/friend/pushOrder/contentManagement/insert` | dc-api-office | **新增推单内容** | 🔥🔥🔥 |
| 5 | `/api/friend/pushOrder/contentManagement/updateState` | dc-api-office | **更新推单状态**（发布/下架/完成） | 🔥🔥 |
| 6 | `/api/friend/pushOrder/contentManagement/pageList` | dc-api-office | 推单内容列表查询 | 🔥🔥 |

#### 第2类：客户端接口（4个）- **移动App端/Web前台**
无需认证 | 面向公众 | 读操作

| # | 接口路径 | 工程模块 | 说明 | 优先级 |
|---|---------|---------|------|--------|
| 7 | `/api/public/pushOrder/findPersonalInfoPageList` | dc-api-office | **查询大师列表**（大师榜展示） | 🔥🔥🔥 |
| 8 | `/api/public/pushOrder/findMonthlyRankingPageList` | dc-api-office | **查询排行榜**（红单榜/连红榜/人气榜） | 🔥🔥🔥 |
| 9 | `/api/public/pushOrder/findPlanDetailed` | dc-api-office | **查询方案详情**（用户查看推单方案） | 🔥🔥🔥 |
| 10 | `/api/public/pushOrder/findMasterDetailed` | dc-api-office | 查询大师详情 | 🔥🔥 |

#### 第3类：三方接口（4个）- **第三方系统**
Token认证 | 支持签名验证 | 读操作为主

| # | 接口路径 | 工程模块 | 说明 | 认证方式 | 优先级 |
|---|---------|---------|------|---------|--------|
| 11 | `/api/public/pushOrder/findMonthlyRankingPageList` | dc-api-office | 三方排行榜查询 | MD5+timestamp | 🔥🔥🔥 |
| 12 | `/api/public/pushOrder/findProgrammePreferredPageList` | dc-api-office | 三方偏好方案查询 | 无需认证 | 🔥🔥 |
| 13 | `/api/public/pushOrder/findMatchPreferredPageList` | dc-api-office | 三方偏好赛事查询 | 无需认证 | 🔥🔥 |
| 14 | `/api/public/pushOrder/findPushUserInfo` | dc-api-office | 三方用户基本信息 | GET/无认证 | 🔥 |

---

### 📈 按数据流向和操作类型分类

| 分类 | 接口数 | 说明 | 例子 |
|-----|--------|------|------|
| **读操作（查询）** | 18个 | 不修改数据，仅用于查询展示 | `pageList`, `findPersonalInfoPageList`, `findMonthlyRankingPageList` |
| **写操作（创建/修改）** | 14个 | 创建或修改数据 | `insert`, `update`, `updateState`, `save`, `del` |
| **异步任务操作** | 3个 | 触发后台异步处理 | `backPushOrder`, `backRestockPushOrder`, `taskCreatePushOrder` |
| **配置操作** | 9个 | 配置管理相关 | `paramConfig`, `levelTitleConfig`, `permissionRecord` |

---

### 🔐 按权限要求分类

| 权限类别 | 接口数 | 认证方式 | 典型接口 |
|---------|--------|---------|---------|
| **需登录认证** | 20个 | Cookie/Session | 后台所有接口（`/api/friend/pushOrder/*`） |
| **无需认证** | 24个 | 无 | 公开接口（`/api/public/pushOrder/*`） |
| **三方签名认证** | 可选 | MD5(key+timestamp) | `openPushOrderData` |

---

## 🏗️ 功能模块架构 - 分层设计

### 工程部署结构

**整个红单功能的44个接口都在同一工程内：**
```
工程：DC-API-2018
├─ 模块：dc-api-office (后端服务模块)
│  ├─ 包：com.dc.it.friend.controller (7个Controller)
│  ├─ 包：com.dc.it.friend.service (6个Service接口 + 多个实现类)
│  ├─ 包：com.dc.it.friend.dao (数据访问层)
│  └─ 包：com.dc.it.friend.pojo (数据模型)
└─ 相关工程：dc-api-parent (共享基础设施)
   ├─ FriendController (朋友圈网关层，可调用pushOrder)
   └─ 数据库表结构定义
```

---

### 🎯 按功能层级分类 - 4层架构

#### 📦 第1层：基础配置层（7个接口）
**职责：** 系统参数、等级配置、权限管理
**特点：** 数据变化不频繁，支持后台配置

| 功能模块 | Controller | 接口数 | 核心接口 | 交互关系 |
|--------|-----------|--------|---------|---------|
| **参数配置** | PushOrderParamConfigController | 3 | `/list`, `/update` | 被其他模块引用 |
| **等级称号** | PushOrderLevelTitleConfigController | 4 | `/list`, `/insert`, `/update` | 供推单内容使用 |
| **权限记录** | PushOrderPermissionRecordController | 1 | `/pageList` | 记录操作历史 |
| 小计 | - | **8个** | - | 基础支撑 |

#### 🔄 第2层：核心业务层（27个接口）
**职责：** AI推单用户管理、推单内容生成与管理
**特点：** 业务逻辑复杂，交互频繁

| 功能模块 | Controller | 接口数 | 核心接口 | 交互关系 |
|--------|-----------|--------|---------|---------|
| **AI用户管理** | PushOrderAiUserController | 15 | `/save`, `/backPushOrder`, `/update` | 触发→内容管理 |
| **内容管理** | PushOrderContentManagementController | 12 | `/insert`, `/updateState`, `/pageList` | 接收AI生成→发布 |
| 小计 | - | **27个** | - | 生产主体 |

#### 📊 第3层：数据记录层（2个接口）
**职责：** 记录推单内容变更、购买统计
**特点：** 被动记录，支持审计

| 功能模块 | Controller | 接口数 | 核心接口 | 交互关系 |
|--------|-----------|--------|---------|---------|
| **内容日志** | PushOrderContentGeneralLogController | 2 | `/pageList`, `/purchaseStatisticsPageList` | 被内容管理调用 |
| 小计 | - | **2个** | - | 审计支持 |

#### 🌐 第4层：对外发布层（7个接口）
**职责：** 面向客户端和三方系统的查询接口
**特点：** 无需认证，读操作，支持公众访问

| 功能模块 | Controller | 接口数 | 核心接口 | 交互关系 |
|--------|-----------|--------|---------|---------|
| **对外API** | PushOrderPublicController | 7 | 全部关键接口 | 消费内容管理的数据 |
| 小计 | - | **7个** | - | 数据展示 |

---

### 🔗 模块间交互与依赖关系

```
┌────────────────────────────────────────────────────────────────────────┐
│                         基础配置层                                      │
├────────────────┬──────────────────────┬─────────────────────────────────┤
│ 参数配置       │   等级配置            │      权限记录                    │
│ (ParamConfig)  │ (LevelTitleConfig)   │  (PermissionRecord)             │
└────────┬───────┴──────────┬───────────┴─────────────────┬───────────────┘
         │                  │                             │
         └──────────────────┼─────────────────────────────┘
                            ▼ 被引用
┌────────────────────────────────────────────────────────────────────────┐
│                       核心业务层（交互最复杂）                            │
├──────────────────────────────┬──────────────────────────────────────────┤
│    AI推单用户管理             │       推单内容管理                      │
│   (PushOrderAiUser)          │  (PushOrderContentManagement)           │
│                              │                                         │
│ • save/update(配置推单者)     │ • insert(创建内容)                     │
│ • backPushOrder(触发生成)🔥   │ • updateState(状态管理)🔥             │
│ • list(查询用户)              │ • update(内容更新)                     │
│ • del(删除用户)               │ • pageList(列表查询)                  │
│ • checkAIUser(检查)           │ • findDetectingUsername(审核)         │
│                              │ • updateGrantKf(客服授权)              │
│                              │ • findGrantKf(查询授权)                │
│                              │ • updatePartialFields(部分更新)        │
│                              │ • findMatchFootBasketBall(赛事)        │
└──────────────────┬───────────┴──────────────────────┬───────────────────┘
                   │                                  │
    触发AI生成     │ 保存生成内容                    │
                   └──────────────────────┬───────────┘
                                          ▼ 变更记录
┌────────────────────────────────────────────────────────────────────────┐
│                         数据记录层                                      │
├────────────────────────────────────────────────────────────────────────┤
│  推单内容日志                                                           │
│  (PushOrderContentGeneralLog)                                          │
│  • pageList(日志查询)                                                  │
│  • purchaseStatisticsPageList(购买统计)                               │
└────────────────────────────┬───────────────────────────────────────────┘
                             │
         数据向下游流动        │
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│                       对外发布层                                        │
├────────────────────────────────────────────────────────────────────────┤
│  推单对外API                                                            │
│  (PushOrderPublicController)                                           │
│                                                                        │
│  • findPersonalInfoPageList(大师列表)     🔥🔥🔥                    │
│  • findMonthlyRankingPageList(排行榜)     🔥🔥🔥                    │
│  • findPlanDetailed(方案详情)             🔥🔥🔥                    │
│  • findMasterDetailed(大师详情)           🔥🔥                     │
│  • findProgrammePreferredPageList(偏好)   🔥🔥                     │
│  • findMatchPreferredPageList(赛事偏好)   🔥🔥                     │
│  • findPushUserInfo(用户信息)             🔥                       │
└────────────────────────────────────────────────────────────────────────┘
```

---

### 🔄 典型业务流转实例

#### 场景1：后台创建推单（完整链路）

```
后台操作员 → 参数配置(设置PUSHORDER_PRODUCT) 
         → 等级配置(设置大师等级)
         → AI用户管理.save(创建推单者)
         → AI用户管理.backPushOrder(触发AI生成) 
            ↓ (内部调用IAIPushOrderService)
         → 内容管理.insert(自动保存生成的内容)
         → 内容日志.(自动记录)
         → 内容管理.updateState(发布/下架)
         → 对外API.findMonthlyRankingPageList(用户查看)
```

#### 场景2：用户查看红单（消费链路）

```
客户端/App → 对外API.findPersonalInfoPageList(查询大师列表)
         → 对外API.findMasterDetailed(查询大师详情)
         → 对外API.findMonthlyRankingPageList(查询排行榜)
         → 对外API.findPlanDetailed(查询推单方案详情)
         ↓
         [数据来源：内容管理模块存储的已发布推单]
```

---

### 📊 模块关键指标

| 维度 | 配置层 | 业务层 | 记录层 | 发布层 |
|-----|--------|--------|--------|--------|
| **接口数** | 8个 | 27个 | 2个 | 7个 |
| **占比** | 18% | 61% | 5% | 16% |
| **认证** | 需要 | 需要 | 需要 | 不需要 |
| **主要用户** | 配置管理员 | 运营人员 | 系统自动 | 客户端/三方 |
| **操作特点** | 低频率 | 高频率 | 自动记录 | 只读 |
| **交互复杂度** | 低 | 高 | 无交互 | 无交互 |
| **依赖关系** | 被引用 | 依赖配置 | 依赖业务 | 依赖业务 |

---

### ⚠️ 关键交互点（易出问题的地方）

1. **AI用户管理 ↔ 内容管理**
   - 风险：backPushOrder触发后若内容生成失败，需要有重试机制
   - 实现：`taskRepairCreatePushOrder()` 和 `taskRestockPushOrder()` 提供修复

2. **参数配置 → 业务流转**
   - 风险：PUSHORDER_PRODUCT未配置会导致生成失败
   - 实现：系统参数检查 + 日志记录

3. **等级配置 → 排行榜展示**
   - 风险：等级变更可能影响排行榜排序结果
   - 实现：缓存更新策略

4. **内容管理 → 对外发布**
   - 风险：状态不同步可能导致用户看到过期内容
   - 实现：对外API只查询状态为"已发布"的内容

---

### 🏢 按工程和Package清晰展示

所有功能都在 **DC-API-2018/dc-api-office** 工程内：

```
DC-API-2018
└── dc-api
    └── dc-api-office
        ├── src/main/java/com/dc/it/friend/
        │   ├── controller/
        │   │   ├── PushOrderAiUserController.java (15个接口)
        │   │   ├── PushOrderContentManagementController.java (12个接口)
        │   │   ├── PushOrderPublicController.java (7个接口)
        │   │   ├── PushOrderParamConfigController.java (3个接口)
        │   │   ├── PushOrderLevelTitleConfigController.java (4个接口)
        │   │   ├── PushOrderContentGeneralLogController.java (2个接口)
        │   │   └── PushOrderPermissionRecordController.java (1个接口)
        │   ├── service/
        │   │   ├── interfaces/
        │   │   │   ├── IAIPushOrderService.java
        │   │   │   ├── IPushOrderAiUserService.java
        │   │   │   ├── IMasterPushOrderContentManagementService.java
        │   │   │   └── ... (共6个Service接口)
        │   │   └── impl/
        │   │       └── (对应的实现类)
        │   ├── dao/
        │   │   ├── PushOrderAiUserDao.java
        │   │   └── PushOrderContentManagementDao.java
        │   └── pojo/
        │       ├── PushOrderAiUser.java
        │       ├── PushOrderContentManagement.java
        │       └── ... (数据模型)
        └── src/main/resources/
            └── mapper/ (MyBatis映射文件)
```

**工程引用关系：**
```
客户端App/Web
    ↓
FriendController (dc-api-parent/dc-api-web)
    ↓ [网关层，可整合pushOrder接口]
PushOrderController系列 (dc-api-office)
    ↓ [调用]
Service层 (dc-api-office)
    ↓ [调用]
DAO层 + 数据库
```

---

### ✅ 角度覆盖度分析

| 分析维度 | 是否覆盖 | 说明 |
|--------|--------|------|
| 用户群体 | ✅ | 后台管理、客户端、三方 三类完全覆盖 |
| 业务流程 | ✅ | 创建→生成→发布→展示 四层完整覆盖 |
| 操作类型 | ✅ | 读、写、异步、配置 四类覆盖 |
| 权限控制 | ✅ | 认证与非认证接口完全分类 |
| 工程模块 | ✅ | 所有接口标注了工程和包路径 |
| 数据流向 | ✅ | 入参/出参/副作用已分析 |
| 优先级/关键度 | ✅ | 使用火焰数量表示 |

**未遗漏的补充维度：**
- 接口间依赖关系：已在流程图中体现
- 异步/同步特性：在业务流程中已标注
- 幂等性要求：已体现在操作类型中

---

## Controller 统计（原始数据）

### 一、红单相关Controller数量：**7个**

| 序号 | Controller名称 | 位置 | 说明 |
|-----|--------------|------|------|
| 1 | PushOrderAiUserController | `/api/friend/pushOrder/aiUser` | 推单AI用户管理 |
| 2 | PushOrderContentManagementController | `/api/friend/pushOrder/contentManagement` | 推单内容管理 |
| 3 | PushOrderContentGeneralLogController | `/api/friend/pushOrder/contentGeneralLog` | 推单内容通用日志 |
| 4 | PushOrderParamConfigController | `/api/friend/pushOrder/paramConfig` | 推单参数配置 |
| 5 | PushOrderLevelTitleConfigController | `/api/friend/pushOrder/levelTitleConfig` | 推单等级称号配置 |
| 6 | PushOrderPermissionRecordController | `/api/friend/pushOrder/permissionRecord` | 推单权限记录 |
| 7 | PushOrderPublicController | `/api/public/pushOrder` | 红单对外系统接口（无需登录） |

---

## 接口统计

### 二、总接口数量：**44个**

#### 细节分布：

| Controller | 接口数 | 占比 |
|-----------|--------|------|
| PushOrderAiUserController | 15 | 34.1% |
| PushOrderContentManagementController | 12 | 27.3% |
| PushOrderPublicController | 7 | 15.9% |
| PushOrderLevelTitleConfigController | 4 | 9.1% |
| PushOrderParamConfigController | 3 | 6.8% |
| PushOrderContentGeneralLogController | 2 | 4.5% |
| PushOrderPermissionRecordController | 1 | 2.3% |

---

## 主要功能模块

### 1. 推单AI用户管理（15个接口）
**关键接口：**
- `/leagueList` - 联赛列表查询
- `/testAi` - AI测试接口
- `/backPushOrder` - 后台推单创建
- `/backRestockPushOrder` - 后台补单
- `/list` - 推单AI用户列表（分页）
- `/checkPlayers` - 检查推单用户
- `/checkAIUser` - 检查AI用户
- `/save` - 新增AI用户
- `/update` - 更新AI用户
- `/del` - 删除AI用户
- `/updateGrantKf` - 更新授权客服
- `/findGrantKf` - 查询授权客服
- `/queryHalfAIApplyProduct` - 查询半AI应用产品
- `/saveHalfAI` - 保存半AI配置
- `/updateHalfAI` - 更新半AI配置

### 2. 推单内容管理（12个接口）
**关键接口：**
- `/pageList` - 推单内容列表（分页）
- `/pageListOne` - 推单内容单页查询
- `/insert` - 新增推单内容
- `/update` - 更新推单内容
- `/updateState` - 更新推单状态
- `/openPushOrderData` - 对外开放推单数据查询
- `/findDetectingUsername` - 查询审核用户名
- `/findGeneralLogPageList` - 查询日志列表
- `/updatePartialFields` - 更新部分字段
- `/findMatchFootBasketBall` - 查询足篮球赛事
- `/updateGrantKf` - 更新授权客服
- `/findGrantKf` - 查询授权客服

### 3. 红单对外系统接口（7个接口 - 无需登录）
**关键接口：**
- `/findPersonalInfoPageList` - 查询大师列表（按称号：足球大师/篮球大师）
- `/findMasterDetailed` - 查询大师详情
- `/findPushUserInfo` - 查询推单用户基本信息
- `/findMonthlyRankingPageList` - 查询月度排行榜（红单榜/连红榜/人气榜）
- `/findPlanDetailed` - 查询方案详情
- `/findProgrammePreferredPageList` - 查询偏好方案列表
- `/findMatchPreferredPageList` - 查询偏好赛事列表

### 4. 推单等级称号配置（4个接口）
**关键接口：**
- `/list` - 等级称号配置列表
- `/insert` - 新增等级称号
- `/update` - 更新等级称号
- `/findIdNameList` - 查询ID-名称列表

### 5. 推单参数配置（3个接口）
**关键接口：**
- `/list` - 参数配置列表
- `/update` - 更新参数配置
- `/findPushOrdersPrice` - 查询推单价格

### 6. 推单内容通用日志（2个接口）
**关键接口：**
- `/pageList` - 日志列表（分页）
- `/purchaseStatisticsPageList` - 购买统计列表

### 7. 推单权限记录（1个接口）
**关键接口：**
- `/pageList` - 权限记录列表

---

## 核心服务实现

### 相关Service类：
- `IAIPushOrderService` - AI推单服务接口
- `IAIService` - AI服务接口
- `IPushOrderAiUserService` - 推单AI用户服务
- `IMasterPushOrderContentManagementService` - 推单内容管理服务
- `IMasterPushOrderParamConfigService` - 推单参数配置服务
- `IMasterPushOrderRealOrderService` - 推单实单服务

### 相关DAO类：
- `PushOrderAiUserDao` - AI用户数据访问
- `PushOrderContentManagementDao` - 内容管理数据访问

---

## 数据模型

### 主要POJO类：
- `PushOrderAiUser` - 推单AI用户
- `PushOrderAiUserCopywrite` - 推单AI用户文案
- `PushOrderContentManagement` - 推单内容管理
- `PushOrderContentGeneralLog` - 推单内容通用日志

---

## 关键特性

### 1. 双AI模式
- **豆包AI**：0号AI类型
- **KIMI AI**：1号AI类型

### 2. 推单产品管理
- 系统参数 `PUSHORDER_PRODUCT` 配置可用的推单产品
- 当前产品代码示例：`at`, `yl` 等

### 3. 推单状态管理
- 0: 未发布
- 1: 已发布
- 2: 已完成
- 等其他状态标识

### 4. 权限管理
- 基于产品和操作用户的权限控制
- 支持客服授权管理

### 5. AI推单任务
- `taskCreatePushOrder()` - 定时创建推单
- `taskRestockPushOrder()` - 定时补单
- `taskRepairCreatePushOrder()` - 修复创建推单
- `taskCreateRealPushOrder()` - 创建真实推单

---

## 调用统计

### 从Friend模块调用
在 FriendController.java 中有对推单的集成：
- 支持关注带推单ID参数：`saveFollowNew` 接口支持 `pushOrderId` 参数

---

## 部署位置

**模块路径：** `DC-API-2018/dc-api/dc-api-office`
**Controller包：** `com.dc.it.friend.controller`
**Service包：** `com.dc.it.friend.service`
**DAO包：** `com.dc.it.friend.dao`

---

## 总结

| 指标 | 数值 |
|------|------|
| **Controller数量** | 7个 |
| **API接口数量** | 44个 |
| **主要业务领域** | 推单内容管理、AI用户管理、对外发布 |
| **用户类型** | 后台操作员、AI推单用户、前端用户、公众 |

红单功能是一个相对独立且完整的子系统，涵盖了推单的整个生命周期：配置管理→AI用户管理→内容管理→对外发布→数据统计。
